PID = 1
Exited = 0 (Exited = 137)


docker run -it 
-i --interactive
-t --tty (TeleTypewriter)
Ctrl + D to exit 

Attach - detach mode: Ctrl + P + Q to detach, to re-attach : Docker attach <container-id>

docker run -d <image> 

docker exec <container_id> <command>
